{
  "name": "lumicore-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --check index.js && node scripts/check-top-level-call-references.js && node scripts/player-save-helpers-test.js && node scripts/tournament-helpers-test.js",
    "serve": "firebase emulators:start --only functions,firestore --project lumicore-95c8a",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions,firestore --project lumicore-95c8a",
    "logs": "firebase functions:log --project lumicore-95c8a"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.1"
  }
}
